// import { getMappedArea } from 'api/api';
// import { areaData } from 'App';

export function migrateNewFeature(): void {
let savedShops: number[] = []
let openShops: number[] = []

// areaList.map(e => (get(e)))

// async function get(value: areaData): Promise<void>{
//     let _response = await getMappedArea('mapped-locations', value.shop.toString())

//     if(_response.success) {
//       if(_response.data) {savedShops.push(_response.data?.shop)}
//     }
//     else if(_response.message === "Not Found") {
//         if(_response.data) {openShops.push(_response.data?.shop)}
//     } else {
//       // ERROR 
//     }
//   }

}